---
import { Clock,MapPin } from "@lucide/astro";

// 1. Definición de la interfaz de Props para TypeScript
interface Props {
  title?: string;
  cont?: string; // Usaremos 'cont' para la descripción
    Icon: any; 
    det?:string;
}



const { title, cont, Icon, det } = Astro.props;
     

---

<div
  class="wrapper text-center w-full max-w-xs sm:max-w-sm md:max-w-md 
         rounded-xl overflow-hidden shadow-lg p-6 sm:p-8 bg-white 
         transition-transform duration-300 ease-in-out 
         hover:scale-105 flex flex-col items-center"
>
  <!-- 💥 1. CÍRCULO CON ICONO 💥 -->
  <div
    class="relative w-16 h-16 sm:w-20 sm:h-20 bg-green-100 rounded-full 
           flex items-center justify-center mb-4 sm:mb-6 
           hover:bg-green-50 transition-colors"
  >
    <Icon class="text-green-600 w-8 h-8 sm:w-10 sm:h-10" />
  </div>

  <!-- 2. TÍTULO -->
  <div class="font-bold text-lg sm:text-xl mb-2 text-gray-900">
    {title}
  </div>

  <!-- 3. DESCRIPCIÓN -->
  <p class="text-gray-700 text-sm sm:text-base mb-4 sm:mb-6 flex-grow">
    {cont}
  </p>
    <div class="flex gap-1 text-blue-400">
            <Clock />
            <div class="text-gray-400">      {det}</div>
     
        </div>

</div>