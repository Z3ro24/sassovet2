---
import Card from "../Card.astro";
import { Phone, MessageCircle, Mail, Clock, MapPin } from "@lucide/astro";

const services = [
  {
    icon: Phone,
    title: "Teléfono", // Corregí el título
    description: "+569 54047440",
  },
  {
    icon: MessageCircle,
    title: "WhatsApp",
    description: "Mensaje Directo",
  },
  {
    icon: Mail,
    title: "Email", // Corregí el título
    description: "feffasas@test.com",
  }
];
---

<section id="contacto" class="relative min-h-[90vh] bg-gradient-to-r from-green-50 to-cyan-200 py-16"aria-label="Medios de contacto">
    <div class="container mx-auto px-4">
        <div class="text-center space-y-4 mb-12 sm:mb-16">
            <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-800">
                Contacto
            </h2>
            <p class="text-base sm:text-lg lg:text-xl text-gray-600 max-w-2xl mx-auto">
                ¿Listo para cuidar a tu mascota? Contáctame y agenda tu consulta hoy mismo
            </p>
        </div>
        
        {/* 💥 AJUSTE CLAVE: Usamos grid-cols-3 en pantallas grandes
            y flex/justify-center para centrar el bloque de tarjetas 💥 */}
        <div class="flex justify-center">
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"> 
                {services.map((service) => (
                    <Card 
                        Icon={service.icon} 
                        title={service.title} 
                        cont={service.description} 
                    />
                ))}
            </div>
        </div>
    </div>
</section>