---
import Card3 from "../Card3.astro";
import { Phone, MessageCircle, Mail, Clock, MapPin } from "@lucide/astro";

const services = [
  {
    icon: Phone,
    title: "Teléfono", // Corregí el título
    description: "+569 54047440",
    action: "tel:+56954047440",
  },
  {
    icon: MessageCircle,
    title: "WhatsApp",
    description: "Mensaje Directo",
    action: "https://wa.me/56954047440",
  },
  {
    icon: Mail,
    title: "Email", // Corregí el título
    description: "f.sassovet@hotmail.com",
    action: "mailto:f.sassovet@hotmail.com",
  }
];
---

<section id="contacto" class="relative min-h-[90vh] bg-gradient-to-r from-green-50 to-cyan-200 py-16"aria-label="Medios de contacto">
    <div class="container mx-auto px-4">
        <div class="text-center space-y-4 mb-12 sm:mb-16">
            <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-primary">
                Contacto
            </h2>
            <p class="text-base sm:text-lg lg:text-xl text-gray-400 max-w-2xl mx-auto">
                ¿List@ para mejorar el cuidado de tu mascota? Contáctame y agenda tu consulta hoy mismo
            </p>
        </div>
        
        {/* 💥 AJUSTE CLAVE: Usamos grid-cols-3 en pantallas grandes
            y flex/justify-center para centrar el bloque de tarjetas 💥 */}
        <div class="flex justify-center">
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"> 
                {services.map((service) => (
                    <Card3 
                        Icon={service.icon} 
                        title={service.title} 
                        value={service.description} 
                        action={service.action}
                    />
                ))}
            </div>
        </div>
    </div>
<div class="container mx-auto px-4 mt-12 max-w-xl">
    <div class="bg-white text-center p-8 rounded-2xl shadow-xl border border-gray-100">
        <h2 class="text-2xl font-bold text-gray-800 mb-2">
            ¿List@ para agendar?
        </h2>
        <p class="text-gray-500 mb-6">
            Contacta ahora y cuida la salud de tu mascota desde casa
        </p>

        <div class="flex justify-center space-x-4">
    <a href="https://wa.me/56954047440" target="_blank" rel="noopener noreferrer" class="wsp flex items-center justify-center bg-green-500 text-white px-4 py-2 rounded-full text-base font-semibold shadow-lg hover:bg-green-600 transition-colors">
        <MessageCircle class="w-4 h-4 mr-2" />
        WhatsApp
      </a>
        
            
         <a href="tel:+56954047440" target="_blank" rel="noopener noreferrer" class="wsp flex items-center justify-center bg-white text-secondary px-4 py-2 rounded-full text-base font-semibold shadow-lg hover:bg-cyan-600 hover:text-white border-2 transition-colors">
        <Phone class="w-4 h-4 mr-2" />
        Llamar
      </a>
        </div>
    </div>
</div>
</section>