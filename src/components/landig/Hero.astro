---
import { Phone, Clock, MapPin } from '@lucide/astro'
import { Image } from 'astro:assets'
import heroImage from '../../assets/foto2.webp'
---

<script>
  // Encuentra todos los botones con la clase `wsp` en la página.
  const buttons = document.querySelectorAll('button.wsp')
  buttons.forEach((button) => {
    button.addEventListener('click', () => {
      window.open('https://wa.me/56954047440')
    })
  })
</script>

<section
  class="relative min-h-[90vh] flex items-center bg-gradient-to-r from-white to-sky-200"
>
  <div class="container mx-auto px-4 py-16 lg:py-24">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Contenido -->
      <div class="space-y-8 animate-fade-in">
        <div class="space-y-4">
          <h1
            class="text-shadow-lg text-3xl sm:text-4xl lg:text-6xl font-bold text-primary leading-tight"
          >
            Veterinaria a domicilio en Rancagua | Machalí
            <span class="block text-secondary text-shadow-lg">
              Cuidado profesional para tus mascotas en casa
            </span>
          </h1>

          <p
            class="text-base sm:text-lg lg:text-xl text-gray-400 leading-relaxed max-w-lg"
          >
            Ofrecemos <strong
              >atención veterinaria a domicilio en Rancagua y Machalí</strong
            >, ideal para quienes buscan un <strong
              >servicio veterinario profesional y personalizado</strong
            > sin mover a su mascota. Evita el estrés del traslado y recibe una <strong
              >consulta veterinaria</strong
            > completa en la comodidad de tu hogar.
            <br />
            <a
              href="#contacto"
              class="text-secondary font-semibold underline hover:text-primary"
            >
              Agenda tu atención veterinaria a domicilio hoy mismo
            </a>.
          </p>
        </div>

        <!-- Info rápida -->
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 text-sm sm:text-base">
          <div class="flex items-center space-x-2 text-vet-gray">
            <MapPin class="w-5 h-5 text-secondary" />
            <span class="text-gray-400">Rancagua y Machalí</span>
          </div>
          <div class="flex items-center space-x-2 text-vet-gray">
            <Clock class="w-5 h-5 text-secondary" />
            <span class="text-gray-400">Horarios flexibles</span>
          </div>
          <div class="flex items-center space-x-2 text-vet-gray">
            <Phone class="w-5 h-5 text-secondary" />
            <span class="text-gray-400">Consultas inmediatas</span>
          </div>
        </div>

        <!-- Botones: CTA a landings por localidad -->
        <div class="flex flex-col sm:flex-row gap-4 mt-4">
          <a
            href="/rancagua"
            class="w-full sm:w-auto flex items-center justify-center bg-blue-600 text-white px-6 py-3 rounded-full text-lg font-semibold shadow-lg hover:bg-blue-700"
          >
            Ver servicio en Rancagua
          </a>

          <a
            href="/machali"
            class="w-full sm:w-auto flex items-center justify-center border-2 border-blue-600 text-blue-600 px-6 py-3 rounded-full text-lg font-semibold hover:bg-blue-600 hover:text-white transition"
          >
            Ver servicio en Machalí
          </a>
        </div>
      </div>

      <!-- Imagen -->
      <div class="relative animate-slide-up mt-8 sm:mt-0">
        <div class="relative rounded-2xl overflow-hidden shadow-hero">
          <Image
            src={heroImage}
            alt="Veterinaria a domicilio en Rancagua y Machalí - Dra. Sasso Vet"
            class="w-full h-[300px] sm:h-[450px] lg:h-[550px] object-cover object-[50%_40%]"
            width={600}
            height={800}
            sizes="(max-width: 600px) 600px, (max-width: 900px) 900px, 1200px"
          />
          <div
            class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"
          >
          </div>
        </div>

        <!-- Floating card -->
        <div
          class="bg-white absolute -bottom-6 -left-6 sm:-left-12 bg-card border-border rounded-xl p-4 sm:p-6 shadow-xl animate-bounce"
        >
          <div class="text-center space-y-1 sm:space-y-2">
            <div class="text-xl sm:text-2xl font-bold text-secondary">+300</div>
            <div class="text-xs sm:text-sm text-gray-400">
              Mascotas atendidas
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
